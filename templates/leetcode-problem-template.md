# {编号4位}. {题目名}

---
编号: {编号}
题目: {题目名}
难度: {简单/中等/困难}
标签: [{标签1}, {标签2}]
来源链接: {LeetCode题目链接}
---

## 题目描述

（用中文完整转述题目要求：输入是什么、要返回什么、关键限制是什么。不要只写一句摘要。）

题目保证：

- （列出影响解法的保证条件，例如：恰好存在一个有效答案。）
- （列出不能忽略的约束，例如：不能重复使用同一个元素。）

### Example 1

```text
Input: ...
Output: ...
Explanation: ...
```

### Example 2

```text
Input: ...
Output: ...
Explanation: ...
```

### 约束条件

- `...`
- `...`

---

## 思路分析

### 突破口

（一句话点明解题关键 insight。例："找两数之和 = 目标值" 等价于遍历时检查 `target - 当前数` 是否已经出现过。）

### 思路拆解

1. **暴力解**：（说明最直接的枚举逻辑及其缺陷。）

2. **问题转化**：（说明从暴力解中发现的可优化点。）

3. **优化方向**：（引出最优解或主解法的核心思想。）

4. **实现要点**：（写具体实现时最容易出错的边界、顺序或数据结构细节。）

### 示意图

（优先使用 ASCII 表格、数组、链表、树结构示意，确保在 Markdown 和 Notion 中都能稳定显示。）

```text
示意过程：
...
```

---

## 复杂度分析

| 解法 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 暴力解 | O(...) | O(...) |
| 优化解 | O(...) | O(...) |

---

## 代码实现

### Go

```go
// 函数功能、参数、返回值说明
// 关键逻辑注释解释"为什么这样做"
```

### Java

```java
// 如使用 Map、List、Queue 等标准库，补全必要 import

class Solution {
    // 函数功能、参数、返回值说明
    // 关键逻辑注释解释"为什么这样做"
}
```

### Python

```python
class Solution:
    # 函数功能、参数、返回值说明
    # 关键逻辑注释解释"为什么这样做"
```

---

## 踩坑记录

- **坑点名称**：（说明常见错误、边界条件或易混淆点。）
- **坑点名称**：（说明为什么会错，以及正确处理方式。）
